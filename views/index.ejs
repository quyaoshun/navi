<% if(!locals.user){ %>
<div class="hreo-unit">
    <h1>blog of beforeload</h1>
    <p>beforeload...</p>
    <p>
	  No idea just for fun.
    </p>
</div>
<% include posts %>
<% } else { %>
    <% include say %>
<% } %>

